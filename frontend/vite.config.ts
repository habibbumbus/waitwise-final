import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  optimizeDeps: {
    exclude: ['lucide-react'],
  },
  // dev server proxy: forward /api/* to the backend running on localhost:8000
  // This allows the frontend to call '/api/register' (same-origin) while
  // the dev server forwards requests to the FastAPI backend.
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        secure: false,
      },
    },
  },
});
